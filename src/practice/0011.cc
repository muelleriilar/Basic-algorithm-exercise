#include <bits/stdc++.h>
using namespace std;
using gg=long long;
int main(){
    // 解除 c、c++ io 同步
    ios::sync_with_stdio(false);
    // 解除 cin cout 同步
    cin.tie(0);
    cout<<setfill('0')<<setw(4)<<5<<'\n';
    return 0;
}