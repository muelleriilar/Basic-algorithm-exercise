#include <bits/stdc++.h>
using namespace std;
using gg = long long;
int main() {
  // 解除 c、c++ io 同步
  ios::sync_with_stdio(false);
  // 解除 cin cout 同步
  cin.tie(0);
  cout<<setfill('0')<<setw(4)<<5<<'\n';
  // 测试echo用的
  // int a;
  // cin >> a;
  // vector<int> b;
  // b.resize(a);
  // for (auto &e : b)
  //   cin >> e;
  // for (auto e:b)
  //   cout << e;
  // cout << endl;
  return 0;
}